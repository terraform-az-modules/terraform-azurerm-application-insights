name: Dependabot 
on:
  workflow_dispatch:

jobs:
  sword-pulling:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repos code
        uses: actions/checkout@v3

      - name: the excalibur
        env:
          THE_SECRET: ${{ secrets.THE_SECRET_YOU_WISH_TO_UNCOVER }}
        run: python scripts/the-secret.py